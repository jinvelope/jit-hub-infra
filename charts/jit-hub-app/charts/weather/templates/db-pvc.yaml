{{- if .Values.global.nfs.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: weather-db-pvc
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: weather-db-pv
  resources:
    requests:
      storage: {{ .Values.db.storage }}
{{- end }}
