# gitops/values/eks-b/monitoring-stack-values.yaml
# EKS-B (DR) = 에이전트 (메트릭/로그를 온프레미스 중앙으로 전송)

kube-prometheus-stack:
  enabled: true

  prometheus:
    prometheusSpec:
      externalLabels:
        cluster: eks-b
      # 온프레미스 중앙 Prometheus로 메트릭 전송 (Tailscale 경유 MetalLB 직접 IP)
      remoteWrite:
        - url: "http://172.16.8.32:9090/api/v1/write"

  grafana:
    enabled: false

  alertmanager:
    enabled: false

loki:
  enabled: false

lokiLoadBalancer:
  enabled: false

promtail:
  enabled: true
  config:
    clients:
      # 온프레미스 중앙 Loki로 로그 전송 (Tailscale 경유 MetalLB 직접 IP)
      - url: "http://172.16.8.34:3100/loki/api/v1/push"
        external_labels:
          cluster: eks-b
