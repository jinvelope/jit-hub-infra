# gitops/values/onprem/cloudflared-values.yaml
replicaCount: 0 # 평시 standby — 모니터링은 MetalLB+Tailscale로 접근하므로 Active 전환 불필요
tunnelId: "xxxx-xxxx-xxxx-xxxx"
credentialsSecret: "cloudflared-creds"
ingress:
  - hostname: "leechs.shop"
    service: http://gateway-service.jit-hub.svc.cluster.local:80
